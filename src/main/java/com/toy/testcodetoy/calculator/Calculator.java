package com.toy.testcodetoy.calculator;

public class Calculator {

    public static int plus(int a1, int a2) {
        // #1
//        return 0;

        // #2
//        return 3;
        // #3
//        if (a1 == 4 && a2 == 1) {
//            return 5;
//        } else {
//            return 3;
//        }

        // #4
        return a1 + a2;

        /**
         * 테스트 코드? 기능을 검증하는 테스트 코드를 먼저 작성하고, 테스트를 통과시키기 위해 개발을 진행!
         *
         * 위 예시처럼 테스트코드를 처음 작성할 때는 점진적으로 구현을 완성해나가는 연습이 필요하다.
         *
         * 그리고 나는 src/main 경로에 이 클래스를 처음 만들고 테스트코드를 작성했는데,
         * 책에서는 배포가 될 이 경로가 아닌 src/test 경로에 이 클래스를 만들고 테스트가 완료되면 이 경로로 옮기는 것을 추천하고 있다.
         * 이렇게 하면 완성되지 않은 코드가 배포되는 것을 방지할 수 있다고 한다! 오호
         */
    }
}
